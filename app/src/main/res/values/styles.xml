<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="android:fontFamily">@font/lobster</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/back_color</item>
        <item name="colorPrimaryDark">@color/back_color</item>
        <item name="colorAccent">@color/back_color</item>
        <item name="android:background">@drawable/splash</item>
        <item name="android:navigationBarColor">@color/back_color</item>
    </style>

    <style name="TextViewDetailStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginLeft">@dimen/layout_margin_left</item>
        <item name="android:padding">@dimen/layout_padding</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/mini_text_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/lobster</item>
    </style>

</resources>